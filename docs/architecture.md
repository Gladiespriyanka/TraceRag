# Architecture
API -> query decomposition -> hybrid retrieval -> RRF -> reranking -> evidence primitives -> grounded generation -> React evidence workspace.
The repository is a modular monolith with explicit service boundaries.
