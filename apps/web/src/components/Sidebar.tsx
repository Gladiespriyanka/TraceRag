import{Database,FileText,GitBranch,LayoutDashboard,Search}from"lucide-react";
export default function Sidebar({active,onChange}:{active:string;onChange:(x:string)=>void}){const items=[["dashboard","Dashboard",LayoutDashboard],["query","Query",Search],["sources","Sources",FileText],["graph","Evidence Graph",GitBranch]] as const;return <aside className="sidebar"><div className="brand"><div className="brand-mark">T</div><div><b>TraceRAG</b><small>Evidence Intelligence</small></div></div><div className="nav-label">WORKSPACE</div>{items.map(([id,label,Icon])=><button className={`nav-item ${active===id?"active":""}`} onClick={()=>onChange(id)} key={id}><Icon size={17}/>{label}</button>)}<div className="side-status"><span className="dot"/>System online<br/><small>Postgres · pgvector</small></div></aside>}
